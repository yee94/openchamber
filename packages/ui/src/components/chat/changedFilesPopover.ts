import {
    statusBarPopoverClassName,
    statusBarPopoverStyle,
} from './statusBarPopover';

export const changedFilesPopoverClassName = `${statusBarPopoverClassName} origin-[var(--transform-origin)]`;

export const changedFilesPopoverStyle = statusBarPopoverStyle;
