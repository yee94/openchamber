/**
 * OpenCode v2 session sharing is not available in this cut.
 * Hide share entry points; never reconstruct a share from local assistant mirrors.
 */
export const SESSION_SHARING_AVAILABLE = false

export const isSessionSharingAvailable = (): boolean => SESSION_SHARING_AVAILABLE
