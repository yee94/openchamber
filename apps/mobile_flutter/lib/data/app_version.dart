/// Product version shown in About. Matches root `package.json` on main
/// (`1.19.3-beta.5`, commit `3a164b6`). Native client version is reported
/// separately from a connected instance's OpenChamber / OpenCode versions.
class AppVersion {
  static const String display = '1.19.3-beta.5';
  static const String baselineCommit = '3a164b6';
  static const String appName = 'OpenChamber';
  static const String applicationId = 'com.yee94.openchamber';
}
